#!/usr/bin/python3

try:
    from sys import argv
    from musikla import GuiApplication, CliApplication
    import asyncio

    if __name__ == "__main__":
        if '--gui' in argv or '-g' in argv:
            GuiApplication().run()
        else:
            asyncio.run( CliApplication( argv[ 1: ] ).run() )
except KeyboardInterrupt:
    pass
